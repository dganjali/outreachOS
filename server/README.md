# OutreachOS Backend

Express + TypeScript + Prisma + Postgres API for OutreachOS MVP.

## Env
This environment blocks creating `.env` / `.env.example` files via tooling, so we keep an env template at `server/env.example`.

For local dev, copy it to `server/.env` manually and fill in values.
